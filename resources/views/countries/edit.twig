{% extends 'templates/app.twig' %}

	{% block content %}
<div class="row">
	<div class="col-md-10">
		<div id="postlist">
			<div class="panel">
                <div class="panel-heading">
                    <div class="text-center">
                        <div class="row">
                            <div class="col-sm-9">
                                <h3 class="pull-left">{{ country.name }} - {{ country.code }}</h3>
                            </div>
                           
                        </div>
                    </div>
                </div>
                
            <div class="panel-body">
                <div class="login-form" >
          <form action="{{ path_for('countries.edit', {'id': country.id}) }}" method="post" autocomplete="off"> 
            <div class="form-group {{ errors.name ? 'has-error' : '' }}">
              <input type="text" class="form-control login-field" value="{{ country.name }}"  name="name" placeholder="Enter  Country Name" >
              <label class="login-field-icon fui-user" for="country-name"></label>
              {% if errors.name %}
              	<span class="help-block">{{ errors.name | first }}</span>
              {% endif %}
            </div>
            
           <div class="form-group {{ errors.code ? 'has-error' : '' }}">
              <input type="text" class="form-control login-field" value="{{ country.code }}"  name="code" placeholder="Enter  Country Code" >
              <label class="login-field-icon fui-user" for="country-code"></label>
              {% if errors.code %}
              	<span class="help-block">{{ errors.code | first }}</span>
              {% endif %}
            </div>
           
            <button class="btn btn-primary btn-lg" type="submit">Save Changes</button>
            
             {{ csrf.field | raw }}
            </form>
          </div>
            </div>
            
        </div>
        
       </div>
    </div>
		<div class="text-center">
            <a href="{{ path_for('posts.add') }}" class="btn btn-primary">New Country</a>
        </div>
    </div>
        <div class="col-md-1"></div>
        <div class="col-md-3">
        </div>
        <div class="col-md-1">
        </div>
    </div>
{% endblock %}
